version: 1
project:
  name: onchain-volume-tracker
  slug: onchain-volume-tracker
  status: preview
repository:
  provider: github
  repo: 0x-m-dev/onchain-volume-tracker
  default_branch: main
architecture:
  language: python
  framework: cli
  frontend: null
database:
  engine: sqlite
services:
  app:
    type: worker
deployment:
  provider: github-pages
  production_url: null
  preview_url: https://0x-m-dev.github.io/onchain-volume-tracker/
discord:
  deploy_log: true
  alerts: ops-alerts
spec:
  path: SPEC.md
